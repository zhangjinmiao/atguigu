spring:
  datasource:
    url: jdbc:mysql://ip:3306/jpa
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
    # 更新或者創建數據表結構
      ddl-auto: update
    # 控制檯顯示sql
    show-sql: true